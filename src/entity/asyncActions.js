export const pending = (baseAction) => (baseAction + "_PENDING");
export const rejected = (baseAction) => (baseAction + "_REJECTED");
export const fulfilled = (baseAction) => (baseAction + "_FULFILLED");
